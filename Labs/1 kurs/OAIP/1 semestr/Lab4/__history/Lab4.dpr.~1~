program Lab4;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

const
N=5;
var
X:array[1..N] of real;
i,v,l:integer;
m:real;
begin
 for i:=1 to n do
 begin
   write('¬ведите элемент массива', 'X[',i,']= ');
   Readln(X[i]);
 end;
 for i:= 1 to n do
 begin
   M := X[i];
   for v := i+1 to n do
    if X[i] = M then
    L := L+1;
   if L <> 0 then
    begin
      writeln(L,', ');
      L:=0;
    end;
    writeln(L)


 end;

end.
